.row
  .small-8.columns
    h1 {{song.title}}
    h4 DOC
    | {{song.doc[0] | date : 'mediumDate'}}
  .small-4.columns
    ul.side-nav.right
      li
        a(href='#') Edit Song
        |   
      li
        a(href='#') Delete Song
            
.row
  .small-6.columns
    h4 Writer(s)
    h6.subheader {{song.writerName.join(', ')}}
    h4 Publisher(s)
    h6.subheader {{song.pubName.join(', ')}}
  .small-6.columns
    h4 Percentages
    h6.subheader {{song.percent.join(', ')}}
    h4 PRO
    h6.subheader {{song.percent.join(', ')}}
